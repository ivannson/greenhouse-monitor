# ThingSpeak channel to read from. Required at runtime on the server.
THINGSPEAK_CHANNEL_ID=
THINGSPEAK_READ_KEY=

# Optional: when running `vite dev` without `vercel dev`, point /api requests
# at a deployed preview URL instead, e.g. https://greenhouse.vercel.app
# VITE_API_BASE=
